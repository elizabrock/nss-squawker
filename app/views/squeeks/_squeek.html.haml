%li
  = link_to("@#{squeek.user.username}", squeek.user) + ": #{squeek.body}" 
  = button_to("Delete", squeek_path(squeek), { method: :delete } )
  
  / # = button_to("Delete", controller: :squeeks, method: :delete,  data: { confirm: "Delete this squeek?" } )
